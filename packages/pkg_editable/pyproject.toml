[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pkg-editable"
version = "0.1.0"
description = "Example package exposed as an editable path dependency"
requires-python = ">=3.11"
readme = "README.md"
